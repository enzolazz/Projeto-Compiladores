programa meu_programa() {%
    char: a;
    float: numero1, numero2;
    a := 'a';
    numero2 := 432E-9;
    numero1 := 1.23; {# Comentario de
    # multiplas linhas.
    #}
    if [ a <> 'b' ] then {%
        {# Fazer alguma coisa #}
        2 + 2;
    %} elseif [ pi = 3 ] then {%
        seila(pi);
    %}
    while [ numero2 < numero1 ] do
        numero2 := numero2 + 1;
%}